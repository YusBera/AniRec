# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
.venv/
venv/
env/

# Local configuration and credentials
.env
config/
token.json
tokens/

# Generated project data
profiles/
cache/
logs/
build/
dist/
release/
# Impeccable's small workflow checkpoint is source handoff data, not build output.
!.impeccable/build/
!.impeccable/build/state.json
# API sidecar build output (PyInstaller). Same reasoning as build/ and dist/:
# generated locally, ~130 MB, never reviewed.
build-api/
dist-api/
/tmp/
*_Profile/
*Profile/
*_Recommended_Anime.csv
*_RecommendationCandidates.csv
*_Genre_Importance.csv
*_Completed_Animes_Imputed.csv

# Tooling and OS noise
.pytest_cache/
.pytest_tmp/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.DS_Store
Thumbs.db
ACCEPTANCE_STATIC_RESULTS.txt

# Local Impeccable decision-server runtime (portable choices live in mock sidecars)
.impeccable/questions/

# Disposable isolated API root used only for UI capture
reports/ui-preservation-2026-09-06/sample-data/
