# Workspace extension — implementation and review scope

2026-09-07. User authorized all five previously listed remaining tasks: saved titles outside feed, remaining Profile sections, real comparison, editable settings, broader/real-account verification. They supplied NeoBalls_ for checks only. No commit/push, deployment, scoring change or real settings/profile mutation is authorized by the checks.

The approved C composition, existing PySide visuals, fonts and Discover behavior remain binding. Every anime poster remains 2:3. C is a composition reference after explicit user approval, not a literal pixel target. Existing completed build-phase state and old 41.1% failed diff are historical; this is a functional extension to established surfaces, not a new comp round.

Implemented:
- GET workspace/library resolves saved IDs from full saved recommendations and existing completed/top/candidate CSV snapshots. Personal match is unavailable for metadata-only titles. POST library/resolve explicitly requests a missing saved title from MAL using configured Client ID. Active profile checked before and after network reads; unsaved IDs refused. Resolved metadata is held for the current page session, not cached persistently.
- Profile renders the existing provider's archetype, high-ranked low ratings, hidden gems, histogram, genres/studios with disclosure title evidence, eras/seasons, habits/timeline. Missing data is explained. Existing higher/lower title evidence stays first. No new statistics algorithm.
- Compare joins local completed CSV with the named public completed list through existing MAL service/mapping. Scores zero/nonfinite/outside 1–10 are unrated. Shared entries sorted by absolute score gap then MAL ID; unrated shown separately. Compatibility remains null/N/A. Counts explicitly describe completed titles returned; NSFW scope explained. Sample remains explicit. Malformed lists and foreign pagination rejected.
- Settings allowlisted validated POST preserves credentials and unexposed fields, prevents unreadable-settings overwrite, checks candidate pool limit, saves via existing service. Save/dirty/discard/error retention supported. Desktop settings do not claim browser theme application.

Verification: frontend CI 56 tests pass; build pass (200.34 kB JS/61.83 gzip, CSS35.66/7.34). 56 scoped Python tests across workspace, boundary, settings/tokens, taste-profile service, MAL mapping pass. Full Python suite still running at packet creation; do not claim full pass.
Real-account read via TestClient + actual network returned HTTP200 for NeoBalls_:168 completed returned,167 shared,167 both-rated, null compatibility. Active local profile was the same NeoBalls_, so this is a same-account local-vs-live snapshot check, not proof of two distinct live users. No real account writes. Synthetic different-user and unrated/private/error tests cover distinct comparison behavior.

UI screenshots under .impeccable/review/extension:
Required: library-desktop.png, library-mobile.png, profile-desktop.png, profile-mobile.png, profile-analysis-desktop.png, compare-desktop.png, compare-mobile.png, settings-desktop.png, settings-mobile.png.
Browser viewport overrides1440x1000 and390x844; exported captures may omit scrollbar and scale viewport shots to1425x990/375x812. Full-page Profile captures1425x6166 and375x8126 include all sections; use image crops as needed for readability. All valid files opened; malformed library mobile fullpage capture replaced with viewport PNG. Profile source is labelled bundled sample. Compare screenshot is missing-connection state in isolated root. Library and Settings use isolated synthetic workspace_test_fixture (Monster absent from feed, present in completed CSV; missingID999999). This is not real account data. Settings change5to7 saved and verified after reload in isolated root. Library source loaded Monster with N/A personal score. Posters measured80x120 mobile, no page overflow375/375. Profile Drama disclosure opened with pointer and toggled via Enter. Existing Compare sample selection still works.

Mechanical detector: extension/detector.json=[] once. Source artifacts frontend/src/workspace, AniRec/api/workspace.py, AniRec/services/workspace_service.py. Preserve DESIGN.md/PySide legacy authority. Do not review old entire migration as unfinished; review this extension against bounded request and truthful limitations.
