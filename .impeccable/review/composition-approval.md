# Concept C authority clarification

User was asked whether C should be accepted as a composition reference rather
than a literal pixel target. The subsequent explanation explicitly stated that
this changes workflow records only: layout, colors, 2:3 posters and capabilities
remain unchanged, while the automatic pixel-matching requirement is removed.

User's explicit approval: **Yap kanka o zaman**.

The supported Impeccable `build-phase advance --force --reason` command accepted
this approval for hero and responsive. Both transitions retain their failed raw
pixel gate results and store the approval reason in `state.json`. No pixel pass
is claimed. Sections and motion record previously implemented and verified work.

`desktop.png` and `mobile.png` are unchanged copies of the independently reviewed
`library-desktop.png` and `library-mobile.png`. Other route captures, the initial
review, the fix verdict and raw diff evidence remain available. This clarification
does not authorize UI changes, full feature parity, publishing, or deployment.
