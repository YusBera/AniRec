# Reference values for local setup. The app reads environment variables from
# your shell; it does not automatically load this file.
# Do not commit real credentials.
MAL_CLIENT_ID=your_client_id_here
MAL_CLIENT_SECRET=your_client_secret_here
MAL_REDIRECT_URI=http://localhost:8080/callback
MAL_TOKEN_FILE=token.json
